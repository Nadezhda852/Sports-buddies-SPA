import React from "react";

const Profile = (props) => {
    return (
        <div>
            <h1> Profile </h1>
        </div>
    );
};

Profile.propTypes = {};

export default Profile;